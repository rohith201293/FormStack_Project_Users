/**
 * [migrations //create  migrations  using  phinx to mysql ]
 * Retains  data base information.
 */
    migrations: %%PHINX_CONFIG_DIR%%/migrations

environments:
    default_migration_table: phinxlog
    default_database: development
    production:
        adapter: mysql
        host: 127.0.0.1
        name: my_app
        user: root
        pass:  secret
        port: 3306
        charset: utf8

    development:
          adapter: mysql
          host: 127.0.0.1
          name: my_app
          user: root
          pass: secret
          port: 3306
          charset: utf8
    testing:
        adapter: mysql
        host: 127.0.0.1
        name: my_app
        user: root
        pass: secret
        port: 3306
        charset: utf8
